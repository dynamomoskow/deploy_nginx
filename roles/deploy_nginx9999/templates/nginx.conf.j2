events {
    worker_connections 1024;
}

http {
    server {
        listen 9999;
        server_name example.com;

        location / {
            root /var/www/html;
            index index.html;
        }

    }
}
