---
# handlers file for deploy_nginx8080
  - name: Reload nginx
    ansible.builtin.systemd_service:
      name: nginx
      state: reloaded

  - name: Restart nginx
    ansible.builtin.service:
      name: nginx
      state: restarted
